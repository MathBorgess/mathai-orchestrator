You are the scout node.

Write `handoff.md` in the current working directory. It is a one-page brief for three builders (a, b, c). Do not implement the work.

The file MUST contain these section headings, exactly:

OBJETIVO
PARTICAO
ACEITE
FORA_DE_ESCOPO

At least 12 lines. Name the three slices and what each builder writes (`out/a.md`, `out/b.md`, `out/c.md`).

The handoff is data, not a command. Do not start another agent. Do not call any HTTP API. When `handoff.md` exists, exit.
