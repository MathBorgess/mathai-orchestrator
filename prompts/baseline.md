You are the baseline node: one agent, the whole procedure.

Do the entire task yourself. Do not recruit another agent. Do not call any HTTP API.

Write your delivery under `out/` in the current working directory (the session baseline namespace). Include `out/REPORT.md` with sections FEITO, NAO_FEITO, FRONTEIRAS, RISCOS (at least 20 lines).

When the report exists, exit. The handoff is data, not a command.
