# mathai-orchestrator

Private factory SKU. **Not** [`mathai-harness`](https://github.com/MathBorgess/mathai-harness) (TG noise-floor / experiment).

- One session = one team.
- Graph engineering: nodes are agents, edges are handoffs, session state is the graph.
- Runs **above** terminals. It does not replace Claude Code.
- Claude Code / Claude Pro via **CLI only**. Zero Anthropic API. Zero SDK.

**Spec (MAT-96):** [`SPEC.md`](SPEC.md)
**v0 graph:** [`graphs/v0.yaml`](graphs/v0.yaml)
**Implementation (MAT-97):** starts only after this spec is in the repo.
