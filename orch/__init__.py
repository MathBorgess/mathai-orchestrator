"""MathAI orchestrator — graph engineering above Claude Code CLI.

This slice (MAT-97 start): `orch doctor` and `orch up` with v1 graph schema,
concurrency stubbed at 1. Claude is invoked only as a CLI subprocess.
"""

__version__ = "0.1.0"
